name: "Ransomware Simulation"
description: "Tests system defenses against encryption attacks without causing harm"
target_apps: [Windows, FileServer]
exec_command: "echo '[INFO] Starting Ransomware Simulation...' && sleep 1 && echo '[SETUP] Loading configuration' && sleep 1 && echo '[WARN] This is a harmless simulation only - no files will be encrypted' && sleep 1 && echo '[INFO] Checking for common security vulnerabilities...' && sleep 2 && echo '[DATA] Scanning for open SMB shares...' && sleep 1 && echo '[VULN] Found 3 shares with open write access' && sleep 1 && echo '[DATA] Checking for outdated OS versions...' && sleep 1 && echo '[VULN] Server2016-Test running without latest security patches' && sleep 1 && echo '[DATA] Testing network segmentation...' && sleep 2 && echo '[DATA] Lateral movement possible between subnets 10.0.1.x and 10.0.2.x' && sleep 1 && echo '[INFO] Testing behavioral detection systems...' && sleep 1 && echo '[ERROR] Failed to execute code due to security controls' && sleep 1 && echo '[INFO] Security control blocked execution successfully' && echo '[INFO] Simulation completed'"
